---
applyTo: "**"
---

# Coding Preferences

- BetterDiscord theme development
- CSS custom properties for theming
- Palenight (Mild Contrast) color scheme preferred
- Rounded corners aesthetic

# Project Architecture

- BetterDiscord theme structure:
  - `/themes/` - Main theme files (.theme.css)
  - `/src/` - Source CSS modules
  - `/build/` - Compiled CSS output
  - `/assets/` - Images and SVG icons
- Theme imports from GitHub Pages
- Uses CSS @import for modular structure

# Solutions Repository

- Color conversion from VSCode theme JSON to CSS variables
- GitHub Pages URL structure: https://[username].github.io/[repo]/[path]
- BetterDiscord theme metadata in CSS comments block
- Custom DM icons via CSS mask-image with SVG URLs
